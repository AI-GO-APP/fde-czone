# aigo 即時磅重看板 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把地磅即時重量從現場電腦送上 aigo,讓網頁即時看板顯示當下磅重(大數字 + 更新時間 + 離線提示)。

**Architecture:** 現場 `reader`(已存在)把即時重量寫到本機 `current-weight.json`;新增獨立 `pusher` 讀檔、按「有車每秒推 / 閒置每分鐘心跳 / reader 過時停推」策略呼叫 aigo action;aigo 用單筆 Custom Object 存目前值;前端每 1.5 秒輪詢並依「aigo 伺服器時間」判斷新鮮度顯示。讀取與推送解耦,網路/斷電不影響現場讀取。

**Tech Stack:** PowerShell(現場 reader/pusher)、Python(aigo actions,pytest)、React + TypeScript(前端,vitest)。

## Global Constraints

以下為全專案規則,每個 task 都隱含適用(數值逐字取自 spec / PLATFORM_NOTES):

- Action 執行 body 必須是 `{"params": {...}}`(不是平的),回信封 `{status, result, error, ...}`。
- Custom Object(x_ 表)只能用 `ctx.db.query_object(slug) / insert_object(slug=, data=) / update_object(slug=, record_id=, data=)`,且該 slug 必須加進 `vfs/scripts/refs.py` 才不會 403。
- 平台**沒有** `ctx.response.error()`;回錯一律 `ctx.response.json({"error": msg})`。
- 台灣時間 = `datetime.utcnow() + timedelta(hours=8)`(同 `weigh.py`)。
- 新鮮度**只用 aigo 伺服器時間** `server_at` / `server_now`;離線門檻 **90 秒**(前端)。pusher:reader 檔案過時門檻 **15 秒**、閒置心跳 **60 秒**。
- 前端輪詢間隔 **1.5 秒**;reader 寫檔:每次重量變動 + 至少每 **5 秒**一次。
- PowerShell 送 HTTP 一律用 UTF-8 bytes + `charset=utf-8`(沿用 `AigoClient.ps1`)。
- 現場程式由「登入自動啟動」排程執行;排程 action 用 `powershell -Command "iex ([IO.File]::ReadAllText('...'))"` 避開 .ps1 執行原則;UserId 用 `[WindowsIdentity]::GetCurrent().Name`。
- 單筆 Custom Object 用固定欄位 `key = "current"` 定位那唯一一筆。
- 範圍為 MVP:只做大數字看板,不做車牌/客戶/磅單清單/推播。

---

## File Structure

**aigo(`aigo-app/`):**
- `vfs/actions/update_live_weight.py`(新)— 寫入/更新目前重量(蓋 server_at)。
- `vfs/actions/get_live_weight.py`(新)— 讀目前重量 + 回 server_now。
- `vfs/actions/manifest.json`(改)— 註冊兩個 action。
- `vfs/scripts/refs.py`(改)— 授權 `x_czone_live_weight`。
- `scripts/setup_tables.py`(改)— 建 `x_czone_live_weight` 物件與欄位。
- `tests/conftest.py`(改)— FakeDB 改為可支援任意 slug。
- `tests/test_live_weight.py`(新)— 兩個 action 的單元測試。

**前端(`aigo-app/vfs/src/`):**
- `aigoClient.ts`(改)— 加 `getLiveWeight()`、`LiveWeight` 型別、`classify()`。
- `aigoClient.test.ts`(改)— `classify()` 單元測試。
- `LiveWeightBoard.tsx`(新)— 看板元件(輪詢 + 顯示)。
- `App.tsx`(改)— 加「看板 / 驗證輸入」切換。

**現場(`LiveWeightReader/`):**
- `Read-LiveWeight.ps1`(改)— 多寫 `current-weight.json`。
- `PushLogic.ps1`(新)— 純決策函式 `Get-PushDecision`(可測)。
- `Push-LiveWeight.ps1`(新)— pusher 主體(讀檔 → 決策 → 推 aigo)。
- `Install-PusherTask.ps1`(新)— pusher 登入自動啟動排程。
- `tests/PushLogic.Tests.ps1`(新)— 決策函式測試(獨立,不需 Pester)。
- `agent/lib/AigoClient.ps1`(改)— 加通用 `Invoke-AigoAction` / `Resolve-AigoAction`。

---

## Task 1: aigo — `update_live_weight` action(含 FakeDB 泛用化)

**Files:**
- Modify: `aigo-app/tests/conftest.py`
- Create: `aigo-app/vfs/actions/update_live_weight.py`
- Test: `aigo-app/tests/test_live_weight.py`

**Interfaces:**
- Produces: action `update_live_weight`,params `{weight:number, state:"weighing"|"idle", at?:string, now?:string}`,回 `{"ok": true}` 或 `{"error": msg}`。寫入 `x_czone_live_weight` 單筆(`key="current"`),含 `server_at`(=`now` 覆寫或伺服器台灣時間)。
- Consumes: `conftest.FakeCtx` / `FakeDB`(泛用版)。

- [ ] **Step 1: 先把 FakeDB 改成支援任意 slug**(現況只認 weighing/vehicle)。以下整段取代 `aigo-app/tests/conftest.py` 內的 `FakeDB` class:

```python
class FakeDB:
    """模擬 ctx.db：Custom Object(x_) 用 query_object 讀、insert_object/update_object 寫。
    以 slug -> rows 的字典儲存，支援任意 x_ 表。"""
    def __init__(self, weighing=None, vehicle=None, live=None):
        self._store = {
            "x_czone_weighing": [dict(r) for r in (weighing or [])],
            "x_czone_vehicle": [dict(r) for r in (vehicle or [])],
            "x_czone_live_weight": [dict(r) for r in (live or [])],
        }
        self.inserted = []
        self.updated = []
    def query_object(self, slug, limit=500, **kw):
        return [dict(r) for r in self._store.get(slug, [])]
    def insert_object(self, slug, data):
        row = {**data, "id": f"new-{len(self.inserted) + 1}"}
        self.inserted.append((slug, row))
        self._store.setdefault(slug, []).append(row)
        return row
    def update_object(self, slug, record_id, data):
        self.updated.append((slug, record_id, data))
        for r in self._store.get(slug, []):
            if r.get("id") == record_id:
                r.update(data)
        return {"success": True}
```

- [ ] **Step 2: 確認既有測試仍過**(泛用化不能弄壞 weigh)

Run: `cd aigo-app && python -m pytest tests/test_weigh_execute.py -q`
Expected: PASS(全綠;若本機無 pytest,見 Task 9 的 Python 路徑)

- [ ] **Step 3: 寫失敗測試** `aigo-app/tests/test_live_weight.py`

```python
import update_live_weight
import get_live_weight
from conftest import FakeCtx, FakeDB


def test_update_inserts_when_empty():
    db = FakeDB(live=[])
    ctx = FakeCtx({"weight": 4830, "state": "weighing", "at": "2026-07-01T14:03:13",
                   "now": "2026-07-01T14:03:14"}, db=db)
    update_live_weight.execute(ctx)
    assert ctx.response.body == {"ok": True}
    assert len(db.inserted) == 1
    slug, row = db.inserted[0]
    assert slug == "x_czone_live_weight"
    assert row["key"] == "current"
    assert row["weight"] == 4830
    assert row["state"] == "weighing"
    assert row["server_at"] == "2026-07-01T14:03:14"


def test_update_updates_existing_current():
    cur = {"id": "rec-1", "key": "current", "weight": 0, "state": "idle",
           "at": "old", "server_at": "old"}
    db = FakeDB(live=[cur])
    ctx = FakeCtx({"weight": 12000, "state": "weighing",
                   "now": "2026-07-01T14:05:00"}, db=db)
    update_live_weight.execute(ctx)
    assert db.updated[0][0] == "x_czone_live_weight"
    assert db.updated[0][1] == "rec-1"
    assert db.updated[0][2]["weight"] == 12000
    assert db.updated[0][2]["server_at"] == "2026-07-01T14:05:00"
    assert len(db.inserted) == 0


def test_update_missing_fields_returns_error():
    ctx = FakeCtx({"state": "weighing"})  # 缺 weight
    update_live_weight.execute(ctx)
    assert ctx.response.body["error"]
```

- [ ] **Step 4: 跑測試確認失敗**

Run: `cd aigo-app && python -m pytest tests/test_live_weight.py -q`
Expected: FAIL(`ModuleNotFoundError: No module named 'update_live_weight'`)

- [ ] **Step 5: 實作** `aigo-app/vfs/actions/update_live_weight.py`

```python
"""寫入/更新目前即時磅重(單筆 x_czone_live_weight，key='current')。
server_at 由本 action 以伺服器台灣時間蓋章，供前端判斷新鮮度(不信任現場時鐘)。"""
from datetime import datetime, timedelta

TABLE = "x_czone_live_weight"
KEY = "current"
TW_OFFSET = timedelta(hours=8)


def _server_now(ctx):
    return ctx.params.get("now") or (datetime.utcnow() + TW_OFFSET).isoformat()


def _find_current(ctx):
    for r in ctx.db.query_object(TABLE, limit=10):
        if r.get("key") == KEY:
            return r
    return None


def execute(ctx):
    p = ctx.params
    weight = p.get("weight")
    state = p.get("state")
    if weight is None or not state:
        ctx.response.json({"error": "缺少 weight 或 state"})
        return
    server_at = _server_now(ctx)
    data = {
        "key": KEY,
        "weight": float(weight),
        "state": state,
        "at": p.get("at") or server_at,
        "server_at": server_at,
    }
    cur = _find_current(ctx)
    if cur:
        ctx.db.update_object(slug=TABLE, record_id=cur["id"], data=data)
    else:
        ctx.db.insert_object(slug=TABLE, data=data)
    ctx.response.json({"ok": True})
```

> 註:測試斷言 `row["weight"] == 4830`,但實作存 `float(weight)` → `4830.0`。Python `4830.0 == 4830` 為 True,斷言通過。

- [ ] **Step 6: 跑測試**(get 的測試此時仍會因 import 失敗,先只跑 update 三個)

Run: `cd aigo-app && python -m pytest tests/test_live_weight.py -q -k update`
Expected: PASS(3 passed)

- [ ] **Step 7: Commit**

```bash
git add aigo-app/tests/conftest.py aigo-app/vfs/actions/update_live_weight.py aigo-app/tests/test_live_weight.py
git commit -m "feat(aigo): update_live_weight action + FakeDB 泛用化"
```

---

## Task 2: aigo — `get_live_weight` action

**Files:**
- Create: `aigo-app/vfs/actions/get_live_weight.py`
- Test: `aigo-app/tests/test_live_weight.py`(補測試)

**Interfaces:**
- Produces: action `get_live_weight`,params `{now?:string}`,回 `{weight, state, at, server_at, server_now}`;無資料回 `{weight: null, state: "idle", at: null, server_at: null, server_now: <台灣時間>}`。

- [ ] **Step 1: 補失敗測試**(加到 `aigo-app/tests/test_live_weight.py` 末端)

```python
def test_get_returns_current_with_server_now():
    cur = {"id": "rec-1", "key": "current", "weight": 4830, "state": "weighing",
           "at": "2026-07-01T14:03:13", "server_at": "2026-07-01T14:03:14"}
    db = FakeDB(live=[cur])
    ctx = FakeCtx({"now": "2026-07-01T14:03:20"}, db=db)
    get_live_weight.execute(ctx)
    b = ctx.response.body
    assert b["weight"] == 4830
    assert b["state"] == "weighing"
    assert b["server_at"] == "2026-07-01T14:03:14"
    assert b["server_now"] == "2026-07-01T14:03:20"


def test_get_empty_returns_nulls_and_idle():
    db = FakeDB(live=[])
    ctx = FakeCtx({"now": "2026-07-01T14:03:20"}, db=db)
    get_live_weight.execute(ctx)
    b = ctx.response.body
    assert b["weight"] is None
    assert b["state"] == "idle"
    assert b["server_at"] is None
    assert b["server_now"] == "2026-07-01T14:03:20"
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd aigo-app && python -m pytest tests/test_live_weight.py -q -k get`
Expected: FAIL(`ModuleNotFoundError: No module named 'get_live_weight'`)

- [ ] **Step 3: 實作** `aigo-app/vfs/actions/get_live_weight.py`

```python
"""讀目前即時磅重(單筆 x_czone_live_weight)，並附上伺服器時間 server_now，
讓前端以同一時間基準(server_now - server_at)判斷新鮮度。"""
from datetime import datetime, timedelta

TABLE = "x_czone_live_weight"
KEY = "current"
TW_OFFSET = timedelta(hours=8)


def _server_now(ctx):
    return ctx.params.get("now") or (datetime.utcnow() + TW_OFFSET).isoformat()


def execute(ctx):
    server_now = _server_now(ctx)
    cur = None
    for r in ctx.db.query_object(TABLE, limit=10):
        if r.get("key") == KEY:
            cur = r
            break
    if not cur:
        ctx.response.json({"weight": None, "state": "idle", "at": None,
                           "server_at": None, "server_now": server_now})
        return
    ctx.response.json({
        "weight": cur.get("weight"),
        "state": cur.get("state", "idle"),
        "at": cur.get("at"),
        "server_at": cur.get("server_at"),
        "server_now": server_now,
    })
```

- [ ] **Step 4: 跑全部 live 測試**

Run: `cd aigo-app && python -m pytest tests/test_live_weight.py -q`
Expected: PASS(5 passed)

- [ ] **Step 5: Commit**

```bash
git add aigo-app/vfs/actions/get_live_weight.py aigo-app/tests/test_live_weight.py
git commit -m "feat(aigo): get_live_weight action"
```

---

## Task 3: aigo — 註冊 action + 授權 + 建表定義

**Files:**
- Modify: `aigo-app/vfs/actions/manifest.json`
- Modify: `aigo-app/vfs/scripts/refs.py`
- Modify: `aigo-app/scripts/setup_tables.py`

**Interfaces:**
- Produces: 平台端可見 `x_czone_live_weight`(欄位 key/weight/at/server_at/state)與兩個 action 的註冊。

- [ ] **Step 1: manifest 加兩個 action**。整段取代 `aigo-app/vfs/actions/manifest.json`:

```json
{
  "actions": [
    {"name": "recognize_plate", "file": "recognize_plate.py", "description": "車牌辨識"},
    {"name": "read_weight", "file": "read_weight.py", "description": "磅數 OCR"},
    {"name": "weigh", "file": "weigh.py", "description": "過磅配對與寫表"},
    {"name": "update_live_weight", "file": "update_live_weight.py", "description": "更新即時磅重"},
    {"name": "get_live_weight", "file": "get_live_weight.py", "description": "讀取即時磅重"}
  ]
}
```

- [ ] **Step 2: refs.py 授權新表**。在 `aigo-app/vfs/scripts/refs.py` 的 `REFS` list 末端(`x_czone_vehicle` 那筆之後、`]` 之前)加入:

```python
    {"table_name": "x_czone_live_weight",
     "columns": ["key", "weight", "at", "server_at", "state"],
     "permissions": ["read", "create", "update"]},
```

- [ ] **Step 3: setup_tables.py 加建表定義**。在 `aigo-app/scripts/setup_tables.py` 的 `TABLES` list 末端(`x_czone_vehicle` 那筆之後、`]` 之前)加入:

```python
    ("x_czone_live_weight", "即時磅重", [
        ("key", "鍵", "text"), ("weight", "重量", "number"),
        ("at", "現場讀取時間", "text"), ("server_at", "伺服器時間", "text"),
        ("state", "狀態", "text"),
    ]),
```

- [ ] **Step 4: 驗證 JSON 合法 + 全測試仍綠**

Run: `cd aigo-app && python -c "import json; json.load(open('vfs/actions/manifest.json', encoding='utf-8')); print('manifest ok')" && python -m pytest -q`
Expected: 印出 `manifest ok`,且 pytest 全綠

- [ ] **Step 5: Commit**

```bash
git add aigo-app/vfs/actions/manifest.json aigo-app/vfs/scripts/refs.py aigo-app/scripts/setup_tables.py
git commit -m "feat(aigo): 註冊 live weight actions + 授權 + 建表定義"
```

---

## Task 4: 前端 — `getLiveWeight()` 與 `classify()`

**Files:**
- Modify: `aigo-app/vfs/src/aigoClient.ts`
- Test: `aigo-app/vfs/src/aigoClient.test.ts`

**Interfaces:**
- Produces:
  - `interface LiveWeight { weight: number|null; state: string; at: string|null; server_at: string|null; server_now: string }`
  - `getLiveWeight(): Promise<LiveWeight>`(呼叫 action `get_live_weight`)
  - `type BoardState = "offline" | "weighing" | "idle"`
  - `classify(lw: LiveWeight, staleSec?: number): BoardState`(預設 staleSec=90)
- Consumes: 既有 `callAction`。

- [ ] **Step 1: 寫失敗測試**(加到 `aigo-app/vfs/src/aigoClient.test.ts` 末端;先補 import)

把第 2 行 import 改為:
```typescript
import { buildActionUrl, unwrapEnvelope, mapRecord, classify, LiveWeight } from "./aigoClient";
```

在檔案末端加:
```typescript
describe("classify", () => {
  const base: LiveWeight = {
    weight: 4830, state: "weighing",
    at: "2026-07-01T14:03:13",
    server_at: "2026-07-01T14:03:14",
    server_now: "2026-07-01T14:03:15",
  };
  it("新鮮 + 有車 → weighing", () => {
    expect(classify(base)).toBe("weighing");
  });
  it("新鮮 + 重量 0 → idle", () => {
    expect(classify({ ...base, weight: 0, state: "idle" })).toBe("idle");
  });
  it("server_at 為 null → offline", () => {
    expect(classify({ ...base, server_at: null, weight: null })).toBe("offline");
  });
  it("超過 90 秒 → offline", () => {
    expect(classify({ ...base, server_at: "2026-07-01T14:00:00", server_now: "2026-07-01T14:01:31" })).toBe("offline");
  });
  it("剛好 90 秒 → 不算離線(idle/weighing)", () => {
    expect(classify({ ...base, server_at: "2026-07-01T14:00:00", server_now: "2026-07-01T14:01:30" })).toBe("weighing");
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd aigo-app/vfs && npx vitest run src/aigoClient.test.ts`
Expected: FAIL(`classify` is not exported / not a function)

- [ ] **Step 3: 實作**。在 `aigo-app/vfs/src/aigoClient.ts` 末端加:

```typescript
export interface LiveWeight {
  weight: number | null;
  state: string;
  at: string | null;
  server_at: string | null;
  server_now: string;
}

export async function getLiveWeight(): Promise<LiveWeight> {
  return await callAction("get_live_weight", {});
}

export type BoardState = "offline" | "weighing" | "idle";

// 新鮮度只用伺服器時間(server_now - server_at)。ISO 無時區也沒關係:
// 兩個時間同格式相減，時區解讀會互相抵銷。
export function classify(lw: LiveWeight, staleSec = 90): BoardState {
  if (!lw || !lw.server_at) return "offline";
  const ageSec = (Date.parse(lw.server_now) - Date.parse(lw.server_at)) / 1000;
  if (isNaN(ageSec) || ageSec > staleSec) return "offline";
  if (lw.state === "weighing" && !!lw.weight && lw.weight !== 0) return "weighing";
  return "idle";
}
```

- [ ] **Step 4: 跑測試**

Run: `cd aigo-app/vfs && npx vitest run src/aigoClient.test.ts`
Expected: PASS(含既有測試全綠)

- [ ] **Step 5: Commit**

```bash
git add aigo-app/vfs/src/aigoClient.ts aigo-app/vfs/src/aigoClient.test.ts
git commit -m "feat(web): getLiveWeight + classify(新鮮度用伺服器時間)"
```

---

## Task 5: 前端 — 即時磅重看板元件 + 切換

**Files:**
- Create: `aigo-app/vfs/src/LiveWeightBoard.tsx`
- Modify: `aigo-app/vfs/src/App.tsx`

**Interfaces:**
- Consumes: `getLiveWeight`, `classify`, `LiveWeight`, `BoardState`(Task 4)。
- Produces: `LiveWeightBoard` React 元件(預設輪詢 1.5 秒)。

- [ ] **Step 1: 實作看板元件** `aigo-app/vfs/src/LiveWeightBoard.tsx`

```tsx
import { useEffect, useState } from "react";
import { getLiveWeight, classify, LiveWeight, BoardState } from "./aigoClient";

const POLL_MS = 1500;

function hhmmss(iso: string | null): string {
  return iso ? iso.slice(11, 19) : "--:--:--";
}

export default function LiveWeightBoard() {
  const [lw, setLw] = useState<LiveWeight | null>(null);
  const [err, setErr] = useState("");

  useEffect(() => {
    let alive = true;
    async function tick() {
      try { const r = await getLiveWeight(); if (alive) { setLw(r); setErr(""); } }
      catch (e: any) { if (alive) setErr(e.message || "讀取失敗"); }
    }
    tick();
    const h = setInterval(tick, POLL_MS);
    return () => { alive = false; clearInterval(h); };
  }, []);

  const state: BoardState = lw ? classify(lw) : "offline";
  const showErr = !!err;

  return (
    <div className="lwb-root">
      <h2 className="lwb-title">薪榮地磅 — 即時磅重</h2>
      {showErr && <div className="lwb-err">連線問題: {err}</div>}
      {state === "offline" && (
        <div className="lwb-offline">
          <div className="lwb-big">⚠️ 離線</div>
          <div className="lwb-sub">最後更新 {hhmmss(lw?.server_at ?? null)}</div>
        </div>
      )}
      {state === "idle" && (
        <div className="lwb-idle">
          <div className="lwb-big">0 <span className="lwb-unit">kg</span></div>
          <div className="lwb-sub">待命中 · 更新於 {hhmmss(lw?.server_at ?? null)}</div>
        </div>
      )}
      {state === "weighing" && (
        <div className="lwb-live">
          <div className="lwb-big">{(lw?.weight ?? 0).toLocaleString()} <span className="lwb-unit">kg</span></div>
          <div className="lwb-sub">更新於 {hhmmss(lw?.server_at ?? null)}</div>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: App.tsx 加「看板 / 驗證輸入」切換**。把 `aigo-app/vfs/src/App.tsx` 最前面兩行 import 改成:

```tsx
import { useEffect, useState } from "react";
import { callAction, listWeighings, WeighingRow } from "./aigoClient";
import LiveWeightBoard from "./LiveWeightBoard";
```

在 `export default function App() {` 之後、`const [plate, ...` 之前加:

```tsx
  const [view, setView] = useState<"board" | "form">("board");
```

把 `return (` 之後那一行 `<div className="wp-root">` 改成加一個切換列與條件顯示:

```tsx
    <div className="wp-root">
      <div className="wp-nav">
        <button className="wp-btn" onClick={() => setView("board")} disabled={view === "board"}>即時看板</button>
        <button className="wp-btn" onClick={() => setView("form")} disabled={view === "form"}>驗證輸入</button>
      </div>
      {view === "board" && <LiveWeightBoard />}
      {view === "form" && (
      <>
        <h2 className="wp-title">薪榮地磅 — 驗證輸入</h2>
```

並在 `App.tsx` 原本最外層 `</div>`(檔案結尾 `  );` 上方那個)之前,把驗證輸入區塊用 `</>` 收起來。也就是原本結尾:

```tsx
        </tbody>
      </table>
    </div>
  );
```
改成:
```tsx
        </tbody>
      </table>
      </>
      )}
    </div>
  );
```

- [ ] **Step 3: 型別檢查 / 建置不壞**

Run: `cd aigo-app/vfs && npx tsc --noEmit`
Expected: 無錯誤(若專案未裝 typescript,改跑 `npx vitest run` 確認既有測試仍綠)

- [ ] **Step 4: Commit**

```bash
git add aigo-app/vfs/src/LiveWeightBoard.tsx aigo-app/vfs/src/App.tsx
git commit -m "feat(web): 即時磅重看板元件 + 看板/表單切換"
```

> 樣式(`lwb-*` class)沿用現有 `App.css` 風格,巨大字體可於 Task 9 微調;MVP 不阻塞於樣式。

---

## Task 6: 現場 reader — 多寫 `current-weight.json`

**Files:**
- Modify: `LiveWeightReader/Read-LiveWeight.ps1`

**Interfaces:**
- Produces: `LiveWeightReader/out/current-weight.json`,內容 `{"weight": <int>, "at": "<ISO 本地>", "state": "weighing"|"idle"}`;每次重量變動寫、且至少每 5 秒寫一次。
- Consumes: 既有 reader C# `Run(durationSec, pollMs, log)`。

- [ ] **Step 1: C# 加寫檔方法**。在 `Read-LiveWeight.ps1` 的 C# 內,`GetCls` 方法之後加一個靜態方法:

```csharp
  static void WriteCurrent(string path, int weight, string state){
    string at = DateTime.Now.ToString("yyyy-MM-ddTHH:mm:sszzz");
    string json = "{\"weight\": " + weight + ", \"at\": \"" + at + "\", \"state\": \"" + state + "\"}";
    try { File.WriteAllText(path, json); } catch {}
  }
```

- [ ] **Step 2: Run 簽章加 jsonPath 參數**。把:

```csharp
  public static void Run(int durationSec, int pollMs, string log){
```
改成:
```csharp
  public static void Run(int durationSec, int pollMs, string log, string jsonPath){
```

- [ ] **Step 3: 迴圈內維護目前值並寫檔**。在 `Run` 內宣告區(`DateTime hb = DateTime.Now;` 那行之後)加:

```csharp
    int curW = 0; string curState = "idle"; DateTime lastWrite = DateTime.MinValue;
    WriteCurrent(jsonPath, curW, curState);
```

在鎖定重量、輸出 `weight:` 那段裡(`if(cv!=activeVal){ activeVal=cv; ... }` 區塊內、寫完 log 之後)加更新目前值:

```csharp
          int pw; if(int.TryParse(cv, out pw)){ curW = pw; curState = (curW==0) ? "idle" : "weighing"; WriteCurrent(jsonPath, curW, curState); lastWrite = DateTime.Now; }
```

在迴圈最後(`System.Threading.Thread.Sleep(pollMs);` 之前)加「至少每 5 秒刷新一次」:

```csharp
      if((DateTime.Now - lastWrite).TotalSeconds >= 5){ WriteCurrent(jsonPath, curW, curState); lastWrite = DateTime.Now; }
```

- [ ] **Step 4: 尾端呼叫傳入 jsonPath**。把檔尾:

```powershell
[WeightReader]::Run(315360000, 300, $log)
```
改成:
```powershell
$jsonPath = Join-Path $logDir 'current-weight.json'
[WeightReader]::Run(315360000, 300, $log, $jsonPath)
```

- [ ] **Step 5: 重啟 reader 並驗證檔案出現**

Run(在對話用 Bash 叫 powershell.exe,或本機終端機):
```
powershell -NoProfile -Command "Restart-ScheduledTask -TaskName ScalesLiveWeightReader; Start-Sleep 8; Get-Content 'C:\Users\user\Desktop\fde-czone\LiveWeightReader\out\current-weight.json'"
```
Expected: 印出類似 `{"weight": 0, "at": "2026-07-01T...+08:00", "state": "idle"}`

- [ ] **Step 6: Commit**

```bash
git add LiveWeightReader/Read-LiveWeight.ps1
git commit -m "feat(LiveWeightReader): reader 多寫 current-weight.json"
```

---

## Task 7: 現場 pusher — 決策函式(TDD)

**Files:**
- Create: `LiveWeightReader/PushLogic.ps1`
- Test: `LiveWeightReader/tests/PushLogic.Tests.ps1`

**Interfaces:**
- Produces: `Get-PushDecision -State <string> -StateChanged <bool> -LastPushEpoch <double> -FileAtEpoch <double> -NowEpoch <double> [-HeartbeatSec 60] [-StaleSec 15]` → 回字串之一:`'stale'`(reader 過時,別推)、`'push'`(有車或狀態剛變,推)、`'heartbeat'`(閒置滿 60 秒,推 0)、`'skip'`(閒置未到心跳,不推)。

- [ ] **Step 1: 寫失敗測試** `LiveWeightReader/tests/PushLogic.Tests.ps1`

```powershell
$ErrorActionPreference = 'Stop'
. (Join-Path $PSScriptRoot '..\PushLogic.ps1')

$fail = 0
function Check($name, $got, $want) {
  if ($got -ne $want) { Write-Host "FAIL $name: got=$got want=$want"; $script:fail++ }
  else { Write-Host "ok   $name" }
}

# reader 檔案過時(now-fileAt > 15) → stale
Check 'reader stale' (Get-PushDecision -State 'weighing' -StateChanged $false -LastPushEpoch 0 -FileAtEpoch 0 -NowEpoch 20) 'stale'
# 有車且檔案新鮮 → push
Check 'weighing push' (Get-PushDecision -State 'weighing' -StateChanged $false -LastPushEpoch 100 -FileAtEpoch 109 -NowEpoch 110) 'push'
# 狀態剛變(weighing→idle,推最後 0) → push
Check 'state changed push' (Get-PushDecision -State 'idle' -StateChanged $true -LastPushEpoch 100 -FileAtEpoch 109 -NowEpoch 110) 'push'
# 閒置未滿 60 秒 → skip
Check 'idle skip' (Get-PushDecision -State 'idle' -StateChanged $false -LastPushEpoch 100 -FileAtEpoch 159 -NowEpoch 160) 'skip'
# 閒置滿 60 秒 → heartbeat
Check 'idle heartbeat' (Get-PushDecision -State 'idle' -StateChanged $false -LastPushEpoch 100 -FileAtEpoch 159 -NowEpoch 160.0001) 'skip'
Check 'idle heartbeat2' (Get-PushDecision -State 'idle' -StateChanged $false -LastPushEpoch 100 -FileAtEpoch 165 -NowEpoch 165) 'heartbeat'

if ($fail -gt 0) { Write-Host "$fail FAILED"; exit 1 } else { Write-Host 'ALL PASS'; exit 0 }
```

> 註:`idle heartbeat` 那筆刻意示範「未滿 60(100→160=60 不算超過,用 -ge 60 才算)」;以 `-ge` 判定,160-100=60 → heartbeat。修正見實作:用 `-ge $HeartbeatSec`。下方 `idle heartbeat2`(65 秒)必為 heartbeat。把 `idle heartbeat` 這筆的期望改成 `heartbeat`:

把上面 `Check 'idle heartbeat' ... 'skip'` 該行改為期望 `'heartbeat'`(60 秒剛好達標):
```powershell
Check 'idle heartbeat' (Get-PushDecision -State 'idle' -StateChanged $false -LastPushEpoch 100 -FileAtEpoch 159 -NowEpoch 160.0001) 'heartbeat'
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `powershell -NoProfile -File LiveWeightReader/tests/PushLogic.Tests.ps1`
Expected: 失敗(`Get-PushDecision` 不存在;或 .ps1 執行原則擋 → 改用 `powershell -NoProfile -Command "iex ([IO.File]::ReadAllText('LiveWeightReader/tests/PushLogic.Tests.ps1'))"`)

- [ ] **Step 3: 實作** `LiveWeightReader/PushLogic.ps1`

```powershell
# 純決策函式:給定狀態與時間,決定要不要推 aigo。無副作用,便於測試。
function Get-PushDecision {
    param(
        [Parameter(Mandatory)][string]$State,
        [Parameter(Mandatory)][bool]$StateChanged,
        [Parameter(Mandatory)][double]$LastPushEpoch,
        [Parameter(Mandatory)][double]$FileAtEpoch,
        [Parameter(Mandatory)][double]$NowEpoch,
        [double]$HeartbeatSec = 60,
        [double]$StaleSec = 15
    )
    if (($NowEpoch - $FileAtEpoch) -gt $StaleSec) { return 'stale' }   # reader 疑似掛了 → 停推
    if ($State -eq 'weighing') { return 'push' }                       # 有車 → 每秒推
    if ($StateChanged) { return 'push' }                              # 剛歸零 → 推最後一筆 0
    if (($NowEpoch - $LastPushEpoch) -ge $HeartbeatSec) { return 'heartbeat' }  # 閒置心跳
    return 'skip'
}
```

- [ ] **Step 4: 跑測試通過**

Run: `powershell -NoProfile -File LiveWeightReader/tests/PushLogic.Tests.ps1`
Expected: `ALL PASS`(exit 0)

- [ ] **Step 5: Commit**

```bash
git add LiveWeightReader/PushLogic.ps1 LiveWeightReader/tests/PushLogic.Tests.ps1
git commit -m "feat(LiveWeightReader): pusher 決策函式 Get-PushDecision + 測試"
```

---

## Task 8: 現場 pusher — 主體 + AigoClient 通用呼叫 + 排程

**Files:**
- Modify: `agent/lib/AigoClient.ps1`
- Create: `LiveWeightReader/Push-LiveWeight.ps1`
- Create: `LiveWeightReader/Install-PusherTask.ps1`

**Interfaces:**
- Consumes: `Get-PushDecision`(Task 7)、`current-weight.json`(Task 6)、aigo action `update_live_weight`(Task 1)、`agent/config.local.json`(既有:aigoBaseUrl/appId/email/password)。
- Produces: 常駐 pusher + 登入自動啟動排程 `ScalesLiveWeightPusher`。

- [ ] **Step 1: AigoClient 加通用 action 呼叫**。在 `agent/lib/AigoClient.ps1` 末端加:

```powershell
function Invoke-AigoAction {
    # 通用:呼叫任一 action,回完整信封 {status, result, error}。
    param([Parameter(Mandatory)]$Cfg, [Parameter(Mandatory)][string]$Token,
          [Parameter(Mandatory)][string]$Action, [Parameter(Mandatory)]$Params)
    $uri = "$($Cfg.aigoBaseUrl)/api/v1/actions/apps/$($Cfg.appId)/run/$Action"
    $body = @{ params = $Params } | ConvertTo-Json -Compress -Depth 8
    $bytes = [System.Text.Encoding]::UTF8.GetBytes($body)
    $resp = Invoke-WebRequest -Method Post -Uri $uri -Headers @{ Authorization = "Bearer $Token" } `
        -Body $bytes -ContentType 'application/json; charset=utf-8' -UseBasicParsing
    return ([System.Text.Encoding]::UTF8.GetString($resp.RawContentStream.ToArray()) | ConvertFrom-Json)
}

function Resolve-AigoAction {
    # 呼叫 action,token 過期自動重登一次;成功回 .result,失敗丟錯。
    param([Parameter(Mandatory)]$Cfg, [Parameter(Mandatory)][string]$Action, [Parameter(Mandatory)]$Params)
    if (-not $script:AigoToken) { $script:AigoToken = Connect-Aigo $Cfg }
    $envelope = $null
    try { $envelope = Invoke-AigoAction $Cfg $script:AigoToken $Action $Params }
    catch { $script:AigoToken = Connect-Aigo $Cfg; $envelope = Invoke-AigoAction $Cfg $script:AigoToken $Action $Params }
    if ($envelope.status -ne 'success') { throw "$Action 失敗: $($envelope.error)" }
    return $envelope.result
}
```

- [ ] **Step 2: 實作 pusher 主體** `LiveWeightReader/Push-LiveWeight.ps1`

```powershell
<#
.SYNOPSIS 地磅即時重量推送 agent — 讀本機 current-weight.json,按策略推 aigo。
.DESCRIPTION
  讀取與推送解耦:reader 只寫本機檔,pusher 負責碰網路。推送失敗僅記 log,
  不影響 reader。由登入自動啟動排程執行(見 Install-PusherTask.ps1)。
#>
$ErrorActionPreference = 'Stop'

$root    = 'C:\Users\user\Desktop\fde-czone'
$jsonPath = Join-Path $root 'LiveWeightReader\out\current-weight.json'
$logPath  = Join-Path $root 'LiveWeightReader\out\pusher.log'
$cfgPath  = Join-Path $root 'agent\config.local.json'

# 防重複執行
$created = $false
$mtx = New-Object System.Threading.Mutex($true, 'Local\ScalesWeightPusher', [ref]$created)
if (-not $created) { return }

. (Join-Path $root 'LiveWeightReader\PushLogic.ps1')
. (Join-Path $root 'agent\lib\AigoClient.ps1')
$Cfg = Get-Content $cfgPath -Raw | ConvertFrom-Json

function Log($m) { Add-Content -Path $logPath -Value ("[{0:yyyy-MM-dd HH:mm:ss}] {1}" -f (Get-Date), $m) }
function NowEpoch { [double]([DateTimeOffset]::Now.ToUnixTimeMilliseconds() / 1000.0) }

Log "pusher started"
$lastPush = 0.0
$prevState = 'idle'

while ($true) {
    Start-Sleep -Seconds 1
    if (-not (Test-Path $jsonPath)) { continue }
    try {
        $cur = Get-Content $jsonPath -Raw | ConvertFrom-Json
    } catch { continue }

    $state = "$($cur.state)"
    $weight = [int]$cur.weight
    $fileAt = try { [double]([DateTimeOffset]::Parse($cur.at).ToUnixTimeMilliseconds() / 1000.0) } catch { NowEpoch }
    $now = NowEpoch
    $changed = ($state -ne $prevState)

    $decision = Get-PushDecision -State $state -StateChanged $changed `
        -LastPushEpoch $lastPush -FileAtEpoch $fileAt -NowEpoch $now
    $prevState = $state

    if ($decision -eq 'push') {
        $params = @{ weight = $weight; state = $state; at = "$($cur.at)" }
    } elseif ($decision -eq 'heartbeat') {
        $params = @{ weight = 0; state = 'idle'; at = "$($cur.at)" }
    } else {
        continue   # skip / stale：不打 API
    }

    try {
        Resolve-AigoAction -Cfg $Cfg -Action 'update_live_weight' -Params $params | Out-Null
        $lastPush = $now
    } catch {
        Log ("push 失敗(略過): " + $_.Exception.Message)   # best-effort,不影響 reader
    }
}
```

- [ ] **Step 3: 實作排程安裝** `LiveWeightReader/Install-PusherTask.ps1`

```powershell
<# 建立 pusher 登入自動啟動排程 ScalesLiveWeightPusher(比照 reader,見 Install-WeightReaderTask.ps1)。#>
$ErrorActionPreference = 'Stop'
$taskName = 'ScalesLiveWeightPusher'
$script   = 'C:\Users\user\Desktop\fde-czone\LiveWeightReader\Push-LiveWeight.ps1'
$me = [System.Security.Principal.WindowsIdentity]::GetCurrent().Name

$cmd = "Invoke-Expression ([IO.File]::ReadAllText('$script'))"
$action = New-ScheduledTaskAction -Execute 'powershell.exe' `
  -Argument "-NoProfile -WindowStyle Hidden -Command `"$cmd`""
$trigger = New-ScheduledTaskTrigger -AtLogOn -User $me
$principal = New-ScheduledTaskPrincipal -UserId $me -LogonType Interactive -RunLevel Limited
$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries `
  -StartWhenAvailable -ExecutionTimeLimit ([TimeSpan]::Zero) `
  -RestartCount 3 -RestartInterval (New-TimeSpan -Minutes 1)

Register-ScheduledTask -TaskName $taskName -Action $action -Trigger $trigger `
  -Principal $principal -Settings $settings -Force | Out-Null
Write-Output "Registered scheduled task: $taskName (trigger: AtLogOn)"
```

- [ ] **Step 4: Commit(先提交程式碼,實跑驗證在 Task 9)**

```bash
git add agent/lib/AigoClient.ps1 LiveWeightReader/Push-LiveWeight.ps1 LiveWeightReader/Install-PusherTask.ps1
git commit -m "feat(LiveWeightReader): pusher 主體 + AigoClient 通用 action + 排程安裝"
```

---

## Task 9: 部署 aigo + 端到端驗證

**Files:** 無(部署與驗證)

**前置:** 本機無 Python,用 `C:\Users\user\tools\python-3.12.10-embed\python.exe`;跑需 UTF-8 主控台(`PYTHONUTF8=1`)。aigo 憑證在 `agent/config.local.json` / `.env`。

- [ ] **Step 1: 建立 aigo 資料表**

Run: `set -a; source aigo-app/.env 2>/dev/null; set +a; PYTHONUTF8=1 python aigo-app/scripts/setup_tables.py`
Expected: 印出 `+ 建立 即時磅重(x_czone_live_weight)` 或 `= ...已存在`,結尾 `✅ 自建表就緒`

- [ ] **Step 2: 上傳 action source + 授權 references**

用**與現有 weigh action 相同的部署方式**(見 `aigo-app/PLATFORM_NOTES.md` §1/§4/§6:PUT source 後 action 即可執行;`ensure_references` 依 `refs.py` 設定授權)。若 repo 內找不到 `deploy.py`,則此步驟需先向平台方確認既有部署腳本位置(部署管線不在本 repo)。
Expected: `update_live_weight` / `get_live_weight` source 上傳成功;`x_czone_live_weight` 授權完成。

- [ ] **Step 3: 驗證 action 可直接呼叫(推一筆再讀)**

Run(本機終端機或對話 Bash→powershell,先 `Connect-Aigo`):
```
powershell -NoProfile -Command "iex ([IO.File]::ReadAllText('C:\Users\user\Desktop\fde-czone\agent\lib\AigoClient.ps1')); $Cfg = Get-Content 'C:\Users\user\Desktop\fde-czone\agent\config.local.json' -Raw | ConvertFrom-Json; Resolve-AigoAction -Cfg $Cfg -Action 'update_live_weight' -Params @{weight=4830; state='weighing'; at='2026-07-01T15:00:00+08:00'} | Out-Null; Resolve-AigoAction -Cfg $Cfg -Action 'get_live_weight' -Params @{} | ConvertTo-Json"
```
Expected: 回 JSON 含 `weight=4830, state=weighing, server_at, server_now`

- [ ] **Step 4: 啟動 pusher 並確認實際推送**

Run: `powershell -NoProfile -Command "iex ([IO.File]::ReadAllText('C:\Users\user\Desktop\fde-czone\LiveWeightReader\Install-PusherTask.ps1')); Start-ScheduledTask -TaskName ScalesLiveWeightPusher; Start-Sleep 8; Get-Content 'C:\Users\user\Desktop\fde-czone\LiveWeightReader\out\pusher.log' -Tail 10"
Expected: pusher.log 有 `pusher started`,且無持續 `push 失敗`;閒置時每分鐘一筆心跳(可再 `get_live_weight` 確認 server_at 有在更新)

- [ ] **Step 5: 前端建置 + 部署,人工看看板**

Run: `cd aigo-app/vfs && npm run build`(依專案 script;若首次發布,依 PLATFORM_NOTES §7 需在 aigo 平台 UI 手動 publish 一次)
Expected: 打開 aigo app → 「即時看板」→ 待命顯示 `0 kg / 待命`;請人在地磅上放車 → 看板 1~2 秒內出現跳動大數字;拔掉網路/停 pusher 超過 90 秒 → 顯示 `⚠️ 離線`

- [ ] **Step 6: 更新記憶 + 收尾 commit**

更新 memory `weighbridge-system.md`:即時磅重看板已上線(pusher 排程 `ScalesLiveWeightPusher`、aigo 表 `x_czone_live_weight`、看板 view)。
```bash
git add -A && git commit -m "chore: 即時磅重看板端到端驗證 + 文件"
```

---

## Self-Review(已於撰寫後檢查)

- **Spec 覆蓋**:§2 架構→Task 6/7/8;§4 資料模型→Task 1/3;§5 推送策略→Task 7/8;§6 actions→Task 1/2/3;§7 前端→Task 4/5;§8 錯誤處理→Task 7(stale/heartbeat)+ Task 4(offline)+ Task 8(best-effort try/catch);§10 測試→Task 1/2/4/7;§11 檔案清單→全 task 對應。
- **型別一致**:`Get-PushDecision` 參數/回傳字串('stale'/'push'/'heartbeat'/'skip')在 Task 7 定義、Task 8 使用一致;`LiveWeight`/`classify`/`BoardState` 在 Task 4 定義、Task 5 使用一致;`update_live_weight` 參數 `{weight,state,at,now}` 在 Task 1 定義、Task 8 送出一致;`x_czone_live_weight` 欄位 key/weight/at/server_at/state 在 Task 1/3 一致。
- **已知風險**:Task 9 Step 2 的部署腳本(deploy.py)不在本 repo,已明確標示需向平台方確認,非隱藏 placeholder。
